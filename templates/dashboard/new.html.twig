{% extends 'layout.html.twig' %}

{% block title %}U spent? - {{ parent() }}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('dashboard_index') }}
{% endblock stylesheets %}

{% block body %}
    <div class="wrapper">
        {% include 'budget/_form.html.twig' with {'titleForm': '💰 Create a budget'} %}
        <hr class="wrapper__separator">
        {% include 'month/_form.html.twig' with {'titleForm': '🗓️ Add a month'} %}
        <hr class="wrapper__separator">
        {% include 'transaction/_form.html.twig' with {'titleForm': '💸 Enter your new transaction'} %}
    </div>
{% endblock body %}
